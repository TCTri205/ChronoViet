{
  "name": "@chronoviet/rag-engine",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "pnpm clean",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "eval": "tsx eval/benchmarks/index.ts",
    "test": "vitest run",
    "test:eval": "vitest run --dir eval/",
    "chat": "tsx src/cli/chat-cli.ts"
  },
  "dependencies": {
    "@chronoviet/shared-spec": "workspace:*",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
