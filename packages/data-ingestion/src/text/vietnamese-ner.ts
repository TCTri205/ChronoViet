/**
 * Vietnamese Historical NER Engine (Stage 1 Candidate Extractor)
 * Re-exported from @chronoviet/shared-spec for backward compatibility.
 */

export {
  type GazetteerEntry,
  GENERIC_EXCLUSION_TERMS,
  sanitizeMarkdownFormatting,
  LEADING_STOPWORDS,
  slugify,
  buildCanonicalId,
  isValidCandidateSpan,
  extractHistoricalCandidateSpans,
} from '@chronoviet/shared-spec';
